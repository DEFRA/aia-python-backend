# Code Cleanup Plan

Analysed against `main` branch post agent-datapipeline-uplift merge.

---

## Phase 1 — Safe in one commit (no production impact)

All 9 items below have zero risk. Apply, run the test suite once, and commit together.

| # | Item | File | Steps |
|---|------|------|-------|
| 1 | Fix broken import in mock script | `scripts/mock_orchestrator.py:55` | Change `from src.handlers.parse import _parse_bytes` → `from src.utils.document_parser import _parse_bytes` |
| 2 | Remove unused imports | `tests/test_upload_router.py:1` | Remove `datetime` and `timezone` from the import line |
| 3 | Delete empty file | `app/agents/evaluation/user.py` | `git rm app/agents/evaluation/user.py` |
| 4 | Remove dead function | `app/agents/evaluation/src/utils/helpers.py:21-49` | Delete `extract_json_array` — defined but never called anywhere in the codebase |
| 5 | Delete superseded script | `scripts/start_dev_server.sh` | `git rm scripts/start_dev_server.sh` — fully superseded by `start-aia.sh`; contains literal `<PASSWORD>` placeholder |
| 6 | Fix stale env var | `app/agents/evaluation/.env.example:5` | Replace `GOVERNANCE_MODEL=""` with `TECHNICAL_MODEL=""` — `GovernanceAgentConfig` was renamed to `TechnicalAgentConfig` |
| 7 | Delete dead comment | `app/core/config.py:15` | Delete `# "CHEDP": ["security", "data", "risk", "ea", "solution"],` — references removed agent types |
| 8 | PID file gitignore | `.gitignore` | Add `.aia.pids` — generated by `start-aia.sh` on every run, should never be committed |
| 9 | Remove locally-deleted tracked files | `docs/SDD_Field Operations Mobile Application_50pc.docx`, `docs/SDD_Legacy Batch Processing System Replacement_0pc.docx` | `git rm` both files |

Commit message: `chore: safe code cleanup — remove dead code, fix stale config, update gitignore`

---

## Phase 2 — Safe after tests pass (verify suite green after each)

Run `pytest tests/ app/agents/evaluation/tests/` after each deletion.

| # | Item | Files | Steps |
|---|------|-------|-------|
| 10 | Delete orphaned production class | `app/services/ingestor_service.py`, `tests/test_ingestor_service.py` | `IngestorService` has zero callers in production — DOCX parsing now goes through `src.utils.document_parser._parse_bytes`. Delete both files. Confirm `python-docx` still required (it is — via `document_parser.py`). |
| 11 | Delete testing-only PDF builder | `app/agents/evaluation/src/utils/pdf_creator.py` | Single-page PDF builder with hardcoded sample data; zero imports across the codebase. `git rm` the file. Update `CLAUDE.md` to remove the reference. |
| 12 | Archive completed plans | `app/agents/evaluation/plans/` | Create `plans/archive/`. Move plans `01` through `11`, `aia-architecture-brainstorm.md`, `code_review_plan.md`, and `plans/README.md` into it. The handlers they describe (`parse.py`, `tag.py`, `extract_sections.py`) no longer exist. |
| 13 | Delete EventBridge + payload offload utilities | `src/utils/eventbridge.py`, `src/utils/payload_offload.py` | Lambda/EventBridge deployment path permanently retired. Delete both files and their tests. |
| 14 | Delete multi-page PDF builder + drop dependency | `app/agents/evaluation/src/utils/pdf_creator_multipage.py`, `requirements.txt` | PDF output is not a future requirement. `git rm` the file, then remove `reportlab` from `requirements.txt`. |
| 16 | Update stale SQS schema doc | `app/agents/evaluation/files/system_input_output_SQS.md` | Rewrite to reflect current schemas: `TaskMessage` (orchestrator → relay), `StatusMessage` (relay → orchestrator), and `AgentResult` / `PolicyDocResult` (relay internal). Remove old field names `docId` and `Final_Summary`. |
| 17 | Delete stale local test SDDs | `docs/SDD_100pct_compliant_ECDP.md` | Delete the `.md` file locally (`rm docs/SDD_100pct_compliant_ECDP.md`). Leave `SDD_*.docx` files as-is (not tracked). Add `docs/SDD_*.md` to `.gitignore` to prevent future accidental commits. |

---

## Phase 3 — Pending clarification

| # | Item | Files | Status |
|---|------|-------|--------|
| 15 | Align local runner with production | `app/agents/evaluation/main.py` | This is the **local dev entry point** (`python app/agents/evaluation/main.py ...`) used to run an agent against a document on your machine. It currently calls `fetch_policy_doc_by_category` (single-doc) instead of `fetch_all_policy_docs_by_category` (multi-doc fan-out), so local results may differ from what Lambda produces in production. Awaiting decision: fix now or scope as separate ticket. |

---

## Do NOT touch — load-bearing production paths

| Path | Reason |
|------|--------|
| `app/agents/evaluation/src/agents/security_agent.py` | Active Lambda/Relay agent |
| `app/agents/evaluation/src/agents/technical_agent.py` | Active Lambda/Relay agent |
| `app/agents/evaluation/src/agents/tagging_agent.py` | Active Lambda/Relay agent |
| `app/agents/evaluation/src/agents/schemas.py` | Pydantic contracts used by all agents + orchestrator |
| `app/agents/evaluation/src/config.py` | All config classes for agents, DB, and handlers |
| `app/agents/evaluation/src/db/questions_repo.py` | All four functions are live (`worker.py` + `main.py`) |
| `app/agents/evaluation/src/handlers/agent.py` | `AGENT_REGISTRY` and `CONFIG_REGISTRY` used by `worker.py` and `main.py` |
| `app/agent_service/worker.py` | Running Agent Service |
| `app/orchestrator/main.py` | Running Orchestrator |
| `app/core/config.py` (`TEMPLATE_AGENTS`) | Drives agent selection across all three services |
| `scripts/hooks/` | Pre-commit hooks — do not remove |
| `app/agents/evaluation/files/aws_event_driven_orchestration.md` | Authoritative Lambda architecture reference (cited in `CLAUDE.md`) |
| `app/agents/evaluation/files/security_policy.md` | Sample input for local runner (`main.py`) |

---

## Notes

- **Zero TODO/FIXME/HACK comments** found across all `.py` files — clean.
- `reportlab` can be dropped from `requirements.txt` once item 14 is actioned.
- **No test duplication** between `app/agents/evaluation/tests/` and `tests/` — they cover different code layers.
- `ruff check --select F401` returns only the one issue in item 2 above — imports are otherwise clean.
