# Runtime dependencies for the evaluation pipeline and question generation Lambda.
# Heavy ML packages (torch, transformers, docling, opencv) used only in the
# docling_pdf_parser.py script are intentionally excluded.

# AWS
boto3>=1.34.0

# LLM provider SDK
anthropic==0.79.0

# Config and validation
pydantic==2.12.5
pydantic-settings==2.12.0
PyYAML==6.0.3
python-dotenv==1.2.1

# Database
asyncpg>=0.29.0
psycopg2-binary>=2.9.0

# Document parsing
PyMuPDF>=1.24.0
python-docx==1.2.0

# PDF report generation
reportlab>=4.2.0

# HTTP (SharePoint / external APIs)
requests>=2.32.0
